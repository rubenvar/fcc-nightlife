extends layout

block content
  form.search-location(action="/api/search-results" method="POST")
    label(for="location")
    input(type="text" name="location" id="location")
    input(type="submit" value="Search")
  ul.results