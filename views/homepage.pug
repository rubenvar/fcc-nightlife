extends layout

block content
  form.search-location(action="/search-results" method="GET")
    input(type="text" name="location" id="location" required)
    input.search(type="submit" value="Search venues for tonight")